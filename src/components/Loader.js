import LoadingCircle from '../assets/loading-circle.gif';

const Loader = () => {
    return (
        <img src={LoadingCircle} alt="loading circle gif" className="loader" />
    )
}

export default Loader;